import { AppBar, Button, Container, Stack, Toolbar, Typography } from '@mui/material';
import { Navigate, Route, Routes, Link } from 'react-router-dom';
import { useAuthStore } from './store';
import Login from './pages/Login'; import Register from './pages/Register'; import PatientProfile from './pages/PatientProfile'; import ClinicSearch from './pages/ClinicSearch'; import ClinicDetail from './pages/ClinicDetail'; import WriteReview from './pages/WriteReview'; import ClinicPatientSearch from './pages/ClinicPatientSearch';

export default function App() { const { role, signOut } = useAuthStore(); return <><AppBar position="static"><Toolbar><Typography variant="h6" sx={{ flexGrow: 1 }}>MedMatch</Typography><Stack direction="row" spacing={1}><Button color="inherit" component={Link} to="/clinics">Clinics</Button>{role === 'Patient' && <Button color="inherit" component={Link} to="/profile">Profile</Button>}{role === 'Clinic' && <Button color="inherit" component={Link} to="/clinic-patients">Patients</Button>}{role ? <Button color="inherit" onClick={signOut}>Sign out</Button> : <Button color="inherit" component={Link} to="/login">Sign in</Button>}</Stack></Toolbar></AppBar><Container maxWidth="md" sx={{ py: 4 }}><Routes><Route path="/login" element={<Login />} /><Route path="/register" element={<Register />} /><Route path="/clinics" element={<ClinicSearch />} /><Route path="/clinics/:id" element={<ClinicDetail />} /><Route path="/review/:clinicId" element={<WriteReview />} /><Route path="/profile" element={<PatientProfile />} /><Route path="/clinic-patients" element={<ClinicPatientSearch />} /><Route path="*" element={<Navigate to="/clinics" replace />} /></Routes></Container></> }
