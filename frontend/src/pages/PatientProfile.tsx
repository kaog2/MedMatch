import { Alert, Button, FormControlLabel, MenuItem, Paper, Stack, Switch, TextField, Typography } from '@mui/material'; import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'; import { useEffect, useState } from 'react'; import { api, Consent, Profile } from '../services/api'; import { ErrorState, Loading } from '../components/PageState';
const empty: Profile = { displayMode: 'Anonymous', diagnoses: [], interventions: [], symptoms: [], languages: [] };
export default function PatientProfile() { const client = useQueryClient(); const profile = useQuery({ queryKey: ['profile'], queryFn: () => api<Profile>('/profile') }); const consent = useQuery({ queryKey: ['consent'], queryFn: () => api<Consent>('/consent') }); const [form, setForm] = useState<Profile>(empty); const [privacy, setPrivacy] = useState<Consent | null>(null); useEffect(() => { if (profile.data) setForm(profile.data); }, [profile.data]); useEffect(() => { if (consent.data) setPrivacy(consent.data); }, [consent.data]); const mutation = useMutation({ mutationFn: async () => { await api('/profile', { method: 'PUT', body: JSON.stringify(form) }); return api<Consent>('/consent', { method: 'PUT', body: JSON.stringify(privacy) }); }, onSuccess: () => client.invalidateQueries({ queryKey: ['profile'] }) }); if (profile.isLoading || consent.isLoading) return <Loading />; if (profile.error || consent.error) return <ErrorState error={profile.error ?? consent.error} />; const array = (value: string) => value.split(',').map((x) => x.trim()).filter(Boolean); return <Paper sx={{ p: 3 }}><Typography variant="h4" gutterBottom>Your profile and privacy</Typography><Stack spacing={2}><TextField select label="How you appear" value={form.displayMode} onChange={(e) => setForm({ ...form, displayMode: e.target.value as Profile['displayMode'] })}><MenuItem value="Anonymous">Anonymous</MenuItem><MenuItem value="Pseudonym">Pseudonym</MenuItem><MenuItem value="RealName">Real name</MenuItem></TextField>{form.displayMode === 'Pseudonym' && <TextField label="Pseudonym" value={form.pseudonym ?? ''} onChange={(e) => setForm({ ...form, pseudonym: e.target.value })} />}{form.displayMode === 'RealName' && <TextField label="Real name" value={form.realName ?? ''} onChange={(e) => setForm({ ...form, realName: e.target.value })} />}<TextField label="City" value={form.city ?? ''} onChange={(e) => setForm({ ...form, city: e.target.value })} /><TextField label="Country" value={form.country ?? ''} onChange={(e) => setForm({ ...form, country: e.target.value })} /><TextField label="Diagnoses" value={form.diagnoses.join(', ')} onChange={(e) => setForm({ ...form, diagnoses: array(e.target.value) })} /><TextField label="Symptoms" value={form.symptoms.join(', ')} onChange={(e) => setForm({ ...form, symptoms: array(e.target.value) })} />{privacy && <><Typography variant="h6">Consent preferences</Typography><FormControlLabel control={<Switch checked={privacy.showProfilePublicly} onChange={(e) => setPrivacy({ ...privacy, showProfilePublicly: e.target.checked })} />} label="Show my profile publicly" /><FormControlLabel control={<Switch checked={privacy.patientsContactMe} onChange={(e) => setPrivacy({ ...privacy, patientsContactMe: e.target.checked })} />} label="Other patients can contact me" /><FormControlLabel control={<Switch checked={privacy.clinicsContactMe} onChange={(e) => setPrivacy({ ...privacy, clinicsContactMe: e.target.checked })} />} label="Clinics can contact me" /><FormControlLabel control={<Switch checked={privacy.dataForSearch} onChange={(e) => setPrivacy({ ...privacy, dataForSearch: e.target.checked })} />} label="Allow my health profile in contact search" /></>} {mutation.error && <Alert severity="error">{mutation.error.message}</Alert>}<Button variant="contained" onClick={() => mutation.mutate()} disabled={mutation.isPending}>{mutation.isPending ? 'Saving…' : 'Save changes'}</Button></Stack></Paper>; }
